// These match the imports map in package.json

/* @internal */
declare module "#hashCode" {
    function hashUnknown(x: unknown): number;
}

/* @internal */
declare module "#hashCodeNative" {
    function hashUnknown(x: unknown): number;
}